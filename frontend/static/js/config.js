/**
 * config.js
 * =========
 * Unico arquivo que voce precisa editar depois de hospedar o backend
 * no Render (ou outro servico).
 *
 * - Rodando tudo localmente (python app.py servindo o frontend tambem):
 *     deixe como '' (string vazia) -> usa o mesmo endereco (localhost:5000).
 *
 * - Em producao (frontend no Netlify, backend no Render):
 *     coloque a URL publica do backend, SEM barra no final. Exemplo:
 *     window.API_BASE_URL = "https://seu-backend.onrender.com";
 */
window.API_BASE_URL = '';
